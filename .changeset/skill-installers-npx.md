---
"vibe-model": patch
"vibe-terrain": patch
---

Keep both skill installers runnable through `npx` as well as `bunx`, and report
the version from each published package manifest instead of a hardcoded value.
