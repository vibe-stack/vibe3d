# @vibe3djs/registry-sdk

Typed helpers for authoring Vibe3D-compatible registries.

```ts
import { defineRegistry, defineRegistryItem, sourceFile } from '@vibe3djs/registry-sdk'
```

The helpers validate registry data against `@vibe3djs/schema` and generate
content hashes for source files distributed by a registry.

Released under the MIT License.
