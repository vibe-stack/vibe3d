// Swap this single export when you want browser and headless capture to load a
// different scene. Both platforms import this exact factory.
export { createRelayShowcase as createActivePlayground } from './relay-showcase.ts'
